%ul.nav.navbar-pills.nav-stacked
  %li.dropdown-header=t :root_actions
  - actions(:root).each do |action|
    %li= link_to wording_for(:menu, action), { action: action.action_name, controller: 'rails_admin/main' }, class: action.pjax? ? "pjax" : ""
